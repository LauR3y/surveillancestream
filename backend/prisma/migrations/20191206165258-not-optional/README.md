# Migration `20191206165258-not-optional`

This migration has been generated by Laurens Verschuere at 12/6/2019, 4:52:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `be_laurensverschuere_myserveillance`.`Recording` DROP COLUMN `recordedAt`,
ADD COLUMN `recordedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191206104520-optional..20191206165258-not-optional
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,14 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("db_url")
 }
 model Recording {
   id String @default(cuid()) @id
   cameraName String
   videoFilePath String @unique
   imageFilePath String? @unique
-  recordedAt DateTime? @default(now())
+  recordedAt DateTime @default(now())
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191206165258-not-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191206165258-not-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
