# Migration `20191206104520-optional`

This migration has been generated by Laurens Verschuere at 12/6/2019, 10:45:20 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `be_laurensverschuere_myserveillance`.`Recording` DROP COLUMN `recordedAt`,
ADD COLUMN `recordedAt` datetime(3)   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191206102007-set-default..20191206104520-optional
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,14 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = env("db_url")
 }
 model Recording {
   id String @default(cuid()) @id
   cameraName String
   videoFilePath String @unique
   imageFilePath String? @unique
-  recordedAt DateTime @default(now())
+  recordedAt DateTime? @default(now())
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191206104520-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191206104520-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
